# هِواية — Supabase

النسخة الجديدة من منصة **هِواية**، وتجمع تطبيق الطالب وتطبيق المشرف وملفات Supabase في مستودع واحد.

> التطبيقات القديمة ما زالت مستقلة وتعمل كما هي. لا يتم تحويل روابط الإنتاج إلى هذه النسخة إلا بعد اكتمال الاختبار.

## الهيكل

- `apps/student` — تطبيق الطالب
- `apps/supervisor` — تطبيق المشرف
- `shared` — الإعدادات والملفات المشتركة
- `supabase/migrations` — نسخ مخطط قاعدة البيانات
- `supabase/functions` — الوظائف الآمنة على الخادم
- `docs` — توثيق الانتقال والاختبار

## قاعدة البيانات

مشروع Supabase المرتبط:

- Project ref: `uvcotcfemolqwkgypyeq`
- Region: Sydney
- Time zone داخل نظام هِواية: `Asia/Riyadh`

## الأمان

- لا يُحفظ `service_role` أو أي مفتاح سري في GitHub أو داخل تطبيقات الويب.
- تستخدم الواجهات المفتاح العام فقط مع Row Level Security.
- العمليات الإدارية الحساسة تنفذ عبر وظائف خادم تتحقق من هوية المشرف.

## خطة الانتقال

1. نقل واجهتي الطالب والمشرف إلى هذا المستودع.
2. ربط تسجيل الدخول الجديد ذي الستة أرقام.
3. ربط النقاط والحضور والمجموعات والترتيب.
4. اختبار النسخة الجديدة على روابط منفصلة.
5. نقل الاستخدام بعد الموافقة، مع إبقاء النسخة القديمة احتياطية.
